切片
切片主要用于取一个list或者tuple的部分元素   字符串同样适用

之前的笨方法  要去n个元素  可以写一个循环 但是代码量比较多

可以使用切片的方法实现

num = [1,2,3,4,5,6,7]
要取前三个元素

num[0:3]  从第0位开始  到下标为3的元素 但是不包括下标为三的元素  下标索引为0的时候可以省略  num[:3] == num[0:3]

切片也支持从后往前取 （-1为最后一个）
  num[-2:]  取最后两个参数  
  num[-2:-1] 去最后一个
  
[::5] 每5个取一个数
