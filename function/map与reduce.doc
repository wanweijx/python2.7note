map函数

map函数需要传递两个参数  第一个参数为函数  第二个参数为序列

map将传入的函数依次作用到序列的每个元素，并把结果作为新的list返回

>>> def f(x):
...     return x * x
...
>>> map(f, [1, 2, 3, 4, 5, 6, 7, 8, 9])
[1, 4, 9, 16, 25, 36, 49, 64, 81]


reduce函数

reduce函数同样需要传递两个参数  第一个参数为函数  第二个参数为序列

reduce可以将每一个序列元素的结果与后面的元素进行累计计算
>>> def add(x, y):
...     return x + y
...
>>> reduce(add, [1, 3, 5, 7, 9])
25

reduce(f, [x1, x2, x3, x4]) = f(f(f(x1, x2), x3), x4) 函数嵌套使用
